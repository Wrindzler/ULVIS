{
  "name": "gordon-freeman",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev:full": "node scripts/dev-full.js",
    "install:all": "npm --prefix kaynak-kodlar/backend install && npm --prefix kaynak-kodlar/frontend install",
    "dev:backend": "npm --prefix kaynak-kodlar/backend run dev",
    "dev:frontend": "npm --prefix kaynak-kodlar/frontend run dev",
    "test": "npm --prefix kaynak-kodlar/backend test",
    "build": "npm --prefix kaynak-kodlar/frontend run build",
    "audit:high": "npm --prefix kaynak-kodlar/backend audit --audit-level=high && npm --prefix kaynak-kodlar/frontend audit --audit-level=high"
  }
}
